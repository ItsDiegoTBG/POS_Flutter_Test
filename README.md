The following is a point of sale app created in Flutter.

It should have login, register, basic authentications, local data storage, things like that. All should be done within a one week time window.
